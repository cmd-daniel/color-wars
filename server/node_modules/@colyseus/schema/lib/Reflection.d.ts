import { ArraySchema } from "@colyseus/schema/lib/types/custom/ArraySchema";
import { Iterator } from "@colyseus/schema/lib/encoding/decode";
import { Encoder } from "@colyseus/schema/lib/encoder/Encoder";
import { Decoder } from "@colyseus/schema/lib/decoder/Decoder";
import { Schema } from "@colyseus/schema/lib/Schema";
/**
 * Reflection
 */
export declare class ReflectionField extends Schema {
    name: string;
    type: string;
    referencedType: number;
}
export declare class ReflectionType extends Schema {
    id: number;
    extendsId: number;
    fields: ArraySchema<ReflectionField>;
}
export declare class Reflection extends Schema {
    types: ArraySchema<ReflectionType>;
    rootType: number;
    /**
     * Encodes the TypeContext of an Encoder into a buffer.
     *
     * @param encoder Encoder instance
     * @param it
     * @returns
     */
    static encode(encoder: Encoder, it?: Iterator): Buffer;
    /**
     * Decodes the TypeContext from a buffer into a Decoder instance.
     *
     * @param bytes Reflection.encode() output
     * @param it
     * @returns Decoder instance
     */
    static decode<T extends Schema = Schema>(bytes: Buffer, it?: Iterator): Decoder<T>;
}
