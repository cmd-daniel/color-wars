import { DataChange } from "@colyseus/schema/lib/decoder/DecodeOperation";
import { Decoder } from "@colyseus/schema/lib/decoder/Decoder";
export declare function getRawChangesCallback(decoder: Decoder, callback: (changes: DataChange[]) => void): void;
